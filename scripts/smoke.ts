// Full smoke test (migrate -> seed -> expense in KES -> forecast -> settle-up
// -> CSV dedupe -> brief) lands in Phase B once lib/services is implemented
// (DESIGN.md §4). Phase A ships this stub so `npm run smoke` exists.
console.log("smoke: phase B");
